# Optional: how many prior messages narrator receives
NARRATOR_MAX_HISTORY=10

# Optional: Gemini integration for semantic guardrails + narration streaming
GEMINI_API_KEY=
GEMINI_MODEL=gemini-flash-latest
